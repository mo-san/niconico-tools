language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
# command to install dependencies
install:
  - pip install requests progressbar2 prettytable pytest pytest-catchlog
  - pip install -e .
# command to run tests
script: tests/test_nicodown.py
